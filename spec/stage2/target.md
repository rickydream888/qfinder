# 目标

优化文件系统树中目录和文件的图标

## 设想

`resources/pkief.material-icon-theme`目录下有这个VSCode主题的SVG图标库`icons`和目录、文件图片配置文件`material.-icons.json`。

请参考根据这个主题的配置，设计一套用于当前项目软件的图标方案，要求和参考的主题一样细致。

只需适配Light mode即可。

`resources`目录中的文件可以复制到需要的地方，但是不能引用这个目录中的内容。该目录不会上传到代码仓库。
